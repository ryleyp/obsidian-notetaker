# Copy this to .env.local and fill in your values
# Your Anthropic API key - get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Optional: default vault path (can also be set in the app UI)
DEFAULT_VAULT_PATH=
